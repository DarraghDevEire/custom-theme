<a class="ActionLink" href="mailto:{{email}}?
    {{~#with cc}}cc={{this}}&{{~/with~}}
    {{~#with bcc}}bcc={{this}}&{{~/with~}}
    {{~#with subject}}subject={{this}}&{{~/with~}}
    {{~#with emailBody}}body={{this}}{{~/with~}}"
    data-social-service="mailto">
    <svg>
        <use href="#icon-mail"></use>
    </svg>
    <span>
        {{format "MailText"}}
    </span>
</a>
